import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Benefits POS – Benefit payments made effortless.",
  description:
    "Analyze baskets, apply eligible benefits, and complete transactions in seconds. The smart POS platform built for independent retailers.",
  openGraph: {
    title: "Benefits POS – Benefit payments made effortless.",
    description:
      "The smart POS terminal and retailer portal that applies benefit payments automatically.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full`}>
      <body className="min-h-full flex flex-col antialiased">{children}</body>
    </html>
  );
}
